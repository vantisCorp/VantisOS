image: gitpod/workspace-rust

tasks:
  - name: Initialize Citadel
    init: |
      sudo apt-get update
      sudo apt-get install -y qemu-system-x86 nasm
      cargo build
    command: cargo run

vscode:
  extensions:
    - rust-lang.rust-analyzer
    - vadimcn.vscode-lldb
